services:
  - type: web
    name: digitaltwin-api
    env: docker
    dockerfilePath: ./Dockerfile.render
    plan: free
    envVars:
      - key: PORT
        value: "10000"
      - key: PYTHONUNBUFFERED
        value: "1"
    healthCheckPath: /api/health
