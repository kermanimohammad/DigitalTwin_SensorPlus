services:
  - type: web
    name: sensor-api
    env: python
    buildCommand: pip install -r render_requirements.txt
    startCommand: gunicorn render_api:app
    plan: free
    envVars:
      - key: PORT
        value: 10000
    healthCheckPath: /api/health
